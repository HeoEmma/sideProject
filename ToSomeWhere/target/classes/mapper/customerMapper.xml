<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toanywhere.dao.CustomerDAO">

	<!-- addNewCustomer -->
	<insert id="addNewCustomer" parameterType="c"> 
		insert into customer (customerId, id, name, email, password)
        	values (customer_idx_seq.nextval, #{id}, #{name}, #{email}, #{password})
	</insert>
	
</mapper>